/**
 * Created by Raju Bomma on 12/3/2015.
 */

'use strict';

define(['angular'],
    function (angular) {
        //return angular.module('mc.core.routes',[])
        //    .config(['$routeProvider', '$locationProvider', function ($routeProvider, $locationProvider) {
        //        $routeProvider.
        //            when('/groups/:userId', {
        //                templateUrl: '../../angular/groups/views/groups-list.html',
        //                controller: 'groupingController'
        //            }).

        //            otherwise({
        //                redirectTo: '/groups'
        //            })

        //        // use the HTML5 History API
        //        $locationProvider.html5Mode({
        //            enabled: true,
        //            requireBase: true
        //        });
        //    }]);
    });